type Message {
  id: ID!
  text: String!
  isRead: Boolean!
  createdAt: String!
  emitter: User!
  receiver: User!
  conversationId: String!
}

type Query {
  messages(conversationId: String!): [Messages]
}
