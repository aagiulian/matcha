type Like {
  id: ID!
  friend: User!
  datetime: String!
  # back: Boolean!
}

type Blocked {
  id: ID!
  friend: User!
  datetime: String!
  # back: Boolean!
}

type Match {
  id: ID!
  friend: User!
  datetime: String!
  # back: Boolean!
}

type Visit {
  id: ID!
  friend: User!
  datetime: String!
  # back: Boolean!
}
